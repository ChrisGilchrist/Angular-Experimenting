import { ToasterDirective } from './toaster.directive';

describe('ToasterDirective', () => {
  it('should create an instance', () => {
    const directive = new ToasterDirective();
    expect(directive).toBeTruthy();
  });
});
